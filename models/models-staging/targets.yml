# ~/.dbt/profiles.yml
my_project:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: oauth
      project: dbt-bigquery-integration
      dataset: dbtproject  # <-- THIS is where your tables are!
      location: US